{
  "name": "batcave",
  "version": "2.0.0",
  "private": true,
  "description": "Central nervous system — unified monorepo",
  "scripts": {
    "dev": "pnpm --filter console dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "status": "node scripts/status.cjs",
    "scaffold": "node scripts/scaffold.cjs",
    "add": "node scripts/add-project.cjs"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
